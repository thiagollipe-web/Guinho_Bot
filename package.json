{
  "name": "guinho-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "benchmark": "node --input-type=module -e \"import { avaliarClassificador, relatorioCalibracao } from './calibration.js'; console.log(relatorioCalibracao(avaliarClassificador()));\""
  }
}
