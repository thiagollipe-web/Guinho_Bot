{
  "name": "guinho-whatsapp-codegemma",
  "version": "2.0.0",
  "private": true,
  "description": "Bot do WhatsApp com Ollama e motor conversacional local híbrido baseado em regras, memória e SLM.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "check": "node --check index.js && node --check src/eliza/pattern-matcher.js && node --check src/eliza/engine.js && node --check src/guinho.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "ollama": "^0.6.3",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  }
}
